# Results Folder

This folder stores lightweight outputs that help explain model quality and project findings.

Typical contents:

- `metrics.json` for headline evaluation metrics
- `feature_importance.csv` for model drivers
- `benchmarks/` for baseline-model comparisons
- `error_analysis/` for segment-level error reports
- `artifacts/` for training outputs when you rerun experiments locally

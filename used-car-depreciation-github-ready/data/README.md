# Data Notes

- `external/` stores raw source files locally and is ignored by Git.
- `processed/` is for cleaned outputs generated by the pipeline.
- `sample/` contains a small GitHub-safe sample dataset.

The original research folder contains much larger source files that are intentionally not committed here.
